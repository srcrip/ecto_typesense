defmodule EctoTypesenseTest do
  @moduledoc false

  use ExUnit.Case

  # NOTE: I'm not sure there's much that can be doctested, but it'd be nice to try.
  # doctest EctoTypesense

  test "1 == 1" do
    assert 1 == 1
  end
end
